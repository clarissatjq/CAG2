<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Pet App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Pet App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addPet">Add new pet</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Pet</h2></div>
        <br>
        <div class="text-center">
          <% if (pet) { %> 
            <form action="/editPet/<%= pet.petId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Pet Name:</label><br>
                <input type="text" id="name" name="name" value = <%= pet.name %> required><br><br>

                <label for="dob">Date of Birth:</label><br>
                <input type="date" id="dob" name="dob" value = <%= pet.dob %> required><br><br>

                <label for="age">Pet Age:</label><br>
                <input type="number" id="age" name="age" value = <%= pet.age %> required><br><br>

                <label for="breed">Pet Breed:</label><br>
                <input type="text" id="breed" name="breed" value = <%= pet.breed %> required><br><br>

                <label for="contact">Contact:</label><br>
                <input type="number" id="contact" name="contact" value = <%= pet.contact %> required><br><br>

                <label for="image">Image:</label><br>
                <input type="text" name="currentImage" value="<%= pet.image %>" disabled><br>
                <img src = "/images/<%= pet.image %>" width="20%"><br><br>

                New image:<br><input type="file" id="image" name="image" accept="image/*" %>"><br><br>

                <button type="submit">Update Pet</button>
            </form>
          <% } else { %>
              <p>No pet found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
